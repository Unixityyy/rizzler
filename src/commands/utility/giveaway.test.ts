// not even gonna ask gemini because im assuming the giveaway command is gonna be complicated once i start writing it
import { test, expect } from 'vitest'

test('the best test ever', () => {
  expect(1).toBe(1)
})